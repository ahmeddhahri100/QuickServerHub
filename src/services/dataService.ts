/**
 * dataService.ts — Legacy compatibility shim
 * All new code uses the Supabase client directly from src/lib/supabase.ts
 * This file is kept for any residual compatibility only.
 */
export const dataService = {};
